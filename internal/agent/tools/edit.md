Edit a file by exact find-and-replace. Single edit: pass `old_string` and `new_string`. Multiple edits in one call: pass `edits` as an array of `{oldText, newText}` objects — each `oldText` must match exactly once and the matched regions must not overlap, and if any edit fails nothing is written. Can also create a file (empty `old_string`) or delete content (empty `new_string`). If the old text differs from the file only in whitespace, the matching lines are still edited and the new text is re-indented to the file's style; the response says when this happened, so verify the result. Read the file first. For whole-file content use write.
